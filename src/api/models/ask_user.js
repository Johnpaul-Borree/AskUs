// import client from './dbConnect';

// const text = 'INSERT INTO user_table(id, username, first_name, last_name, email, hashed_password) VALUES($1, $2, $3, $4, $5, $6)';
// const values = [5, 'brianc', 'bernard', 'Luisa', 'brian.m.carlson@gmail.com', '1233345'];

// client.query(text, values)
//   .then((result) => {
//     console.log(result.rows);
//     // { name: 'brianc', email: 'brian.m.carlson@gmail.com' }
//   })
//   .catch(e => console.error(e.stack));

// export default client;
